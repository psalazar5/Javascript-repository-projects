function displayType(character) {
  // Displaytype(character) = (this)
  var characterType = character.getAttribute("data-character-type"); //gets attribute from html
  alert(characterType + " is in the " + character.innerHTML + " universe! "); //(character) from displpayType(character);
}
